name: zdream
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip

  - black
  - ruff

  - pytorch
  - pytorch-cuda=12.1
  - torchvision
  - torchaudio
  - tensorboard

  - numpy=1.23.5

  - gymnasium
  - onnx

  - python-dotenv

  - pip:
      - stable-baselines3[extra]
      - mlagents
      - protobuf<4.0.0
